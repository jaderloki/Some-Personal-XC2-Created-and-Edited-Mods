// Stores settings for MCM_TestHarness in order to persist across game runs.
class MCM_TestConfigStore extends Object config(ModConfigMenuTest);

var config bool ENABLE_TEST_HARNESS;

var config bool CLICKED;
var config bool CHECKBOX;
var config float SLIDER;
var config string DROPDOWN;
var config string SPINNER;

var config int VERSION;
